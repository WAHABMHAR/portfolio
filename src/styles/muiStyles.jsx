import { createTheme } from "@mui/material";

export const CustomTheme = createTheme({
    palette: {
        primary: {
            main: "#142033",
            white: "#ffffff",

            light: "#fffff5",
        },
        black: "#000000",
        btnColor: "#00acf0",
    },
});
